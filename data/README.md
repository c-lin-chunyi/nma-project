# Data Directory

Downloaded and generated data live here but are not committed to git.

- `raw/`: original downloaded files, including the Neuromatch parquet dataset.
- `processed/`: derived analysis-ready files.
- `cache/`: temporary or reusable local cache files.

The tracked `.gitkeep` files preserve the directory structure.
